(function () {
    angular.module('movie.card')
        .controller('', function ($routeParams) {
            var self = this,
                imdbId = $routeParams.imdbid;
          // TODO
        });
})();
